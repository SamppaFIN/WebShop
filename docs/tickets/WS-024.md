# WS-024 · CLAUDE.md päivitys

**Vaihe:** 7 — Viimeistely | **Riippuvuudet:** Kaikki | **Arvioitu kesto:** 5 min

## Kuvaus
Päivitä `claude.md` sisältämään täydelliset projektikonventiot. LLM-agenttien referenssitiedosto.

## Hyväksymiskriteerit

`claude.md` sisältää:
1. ✅ Identity JSON (Nexus, DeepSeek V4 Pro, GitHub Copilot, Infinite)
2. ✅ Projektin JSON-metadata (WebShop, stack, hakemistorakenne, käytännöt, ympäristömuuttujat)
3. ✅ Koodikäytännöt:
   - TypeScript strict, ei `any`
   - Tailwind-luokkien järjestys
   - Server vs Client Components
   - Server Actionit: `'use server'`, Zod, try/catch
   - Zustand globaaliin tilaan, Context authiin
4. ✅ Nimeämiskäytännöt: komponentit PascalCase, hookit `use`-prefix, Server Actionit `verbNoun`
5. ✅ Kielletyt: `any`, inline-tyylit (pl. motion), `console.log` commiteissa, tarpeettomat abstraktiot
6. ✅ `npm run build` pitää mennä läpi ennen commitia

## Tiedostot

```
claude.md    ← päivitetään
```
